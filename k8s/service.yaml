apiVersion: v1
kind: Service
metadata:
  name: agi-agent-service
  labels:
    app: agi-agent
spec:
  type: ClusterIP
  selector:
    app: agi-agent
  ports:
  - port: 80
    targetPort: 8090
    protocol: TCP
    name: http